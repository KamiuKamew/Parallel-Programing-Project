\documentclass[a4paper]{article}

\input{style/ch_xelatex.tex}
\input{style/scala.tex}

%代码段设置
\lstset{numbers=left,
basicstyle=\tiny,
numberstyle=\tiny,
keywordstyle=\color{blue!70},
commentstyle=\color{red!50!green!50!blue!50},
frame=single, rulesepcolor=\color{red!20!green!20!blue!20},
escapeinside=``
}

\graphicspath{ {images/} }
\usepackage{ctex}
\usepackage{graphicx}
\usepackage{color,framed}%文本框
\usepackage{listings}
\usepackage{caption}
\usepackage{amssymb}
\usepackage{enumerate}
\usepackage{xcolor}
\usepackage{bm} 
\usepackage{lastpage}%获得总页数
\usepackage{fancyhdr}
\usepackage{tabularx}  
\usepackage{geometry}
\usepackage{minted}
\usepackage{graphics}
\usepackage{subfigure}
\usepackage{float}
\usepackage{pdfpages}
\usepackage{pgfplots}
\pgfplotsset{width=10cm,compat=1.9}
\usepackage{multirow}
\usepackage{footnote}
\usepackage{booktabs}

%-----------------------伪代码------------------
\usepackage{algorithm}  
\usepackage{algorithmicx}  
\usepackage{algpseudocode}  
\floatname{algorithm}{Algorithm}  
\renewcommand{\algorithmicrequire}{\textbf{Input:}}  
\renewcommand{\algorithmicensure}{\textbf{Output:}} 
\usepackage{lipsum}  
\makeatletter
\newenvironment{breakablealgorithm}
  {% \begin{breakablealgorithm}
  \begin{center}
     \refstepcounter{algorithm}% New algorithm
     \hrule height.8pt depth0pt \kern2pt% \@fs@pre for \@fs@ruled
     \renewcommand{\caption}[2][\relax]{% Make a new \caption
      {\raggedright\textbf{\ALG@name~\thealgorithm} ##2\par}%
      \ifx\relax##1\relax % #1 is \relax
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##2}%
      \else % #1 is not \relax
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##1}%
      \fi
      \kern2pt\hrule\kern2pt
     }
  }{% \end{breakablealgorithm}
     \kern2pt\hrule\relax% \@fs@post for \@fs@ruled
  \end{center}
  }
\makeatother
%------------------------代码-------------------
\usepackage{xcolor} 
\usepackage{listings} 
\lstset{ 
breaklines,%自动换行
basicstyle=\small,
escapeinside=``,
keywordstyle=\color{ blue!70} \bfseries,
commentstyle=\color{red!50!green!50!blue!50},% 
stringstyle=\ttfamily,% 
extendedchars=false,% 
linewidth=\textwidth,% 
numbers=left,% 
numberstyle=\tiny \color{blue!50},% 
frame=trbl% 
rulesepcolor= \color{ red!20!green!20!blue!20} 
}

%-------------------------页面边距--------------
\geometry{a4paper,left=2.3cm,right=2.3cm,top=2.7cm,bottom=2.7cm}
%-------------------------页眉页脚--------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\lhead{\kaishu \leftmark}
% \chead{}
\rhead{\kaishu 并行程序设计实验报告}%加粗\bfseries 
\lfoot{}
\cfoot{\thepage}
\rfoot{}
\renewcommand{\headrulewidth}{0.1pt}  
\renewcommand{\footrulewidth}{0pt}%去掉横线
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}%标题横线
\newcommand{\HRulegrossa}{\rule{\linewidth}{1.2mm}}
\setlength{\textfloatsep}{10mm}%设置图片的前后间距
%--------------------文档内容--------------------

\begin{document}
\renewcommand{\contentsname}{目\ 录}
\renewcommand{\appendixname}{附录}
\renewcommand{\appendixpagename}{附录}
\renewcommand{\refname}{参考文献} 
\renewcommand{\figurename}{图}
\renewcommand{\tablename}{表}
\renewcommand{\today}{\number\year 年 \number\month 月 \number\day 日}

%-------------------------封面----------------
\begin{titlepage}
    \begin{center}
    \includegraphics[width=0.8\textwidth]{NKU.png}\\[1cm]
    \vspace{20mm}
		\textbf{\huge\textbf{\kaishu{计算机学院}}}\\[0.5cm]
		\textbf{\huge{\kaishu{并行程序设计实验报告}}}\\[2.3cm]
		\textbf{\Huge\textbf{\kaishu{CUDA 编程}}}

		\vspace{\fill}
    
    % \textbf{\Large \textbf{并行程序设计期末实验报告}}\\[0.8cm]
    % \HRule \\[0.9cm]
    % \HRule \\[2.0cm]
    \centering
    \textsc{\LARGE \kaishu{姓名\ :\ 仇科文}}\\[0.5cm]
    \textsc{\LARGE \kaishu{学号\ :\ 2312237}}\\[0.5cm]
    \textsc{\LARGE \kaishu{专业\ :\ 计算机科学与技术}}\\[0.5cm]
    
    \vfill
    {\Large \today}
    \end{center}
\end{titlepage}

\renewcommand {\thefigure}{\thesection{}.\arabic{figure}}%图片按章标号
\renewcommand{\figurename}{图}
\renewcommand{\contentsname}{目录}  
\cfoot{\thepage\ of \pageref{LastPage}}%当前页 of 总页数

\begin{abstract}
    本文探讨了基于CUDA的NTT（数论变换）算法的GPU并行化实现。首先，我们深入分析了CPU多线程与GPU编程在并行粒度选择上的根本差异，设计了极细粒度的GPU线程划分策略，实现了每个线程处理一个蝶形操作的真正并行化。通过预计算旋转因子表彻底消除了循环携带依赖，成功将CPU的粗粒度并行转换为GPU的极细粒度并行。初步性能测试结果表明，GPU在大规模问题上具有优势，然而该优势比理论预期更差，最大加速比仅为1.525倍。我们扩展了测试规模以便确认这一微弱优势是否能随数据规模增大而增大，然而在进行了从n=4到n=1,048,576的全规模性能测试后，我们认为加速比较小的原因来自于代码本身。经过进一步学习与交流，我们最终认为，代码本身存在极大优化空间。然而受限于时间，我们不得不将进一步优化当作下一次实验的内容。
    
\noindent \textbf{关键词：} GPU编程；CUDA；NTT；数论变换；多项式乘法
\end{abstract}

% 生成目录
\clearpage
\tableofcontents
\newpage

\section{前言}

本次实验中，我将在已实现的基于NTT算法的多项式乘法代码的基础上，尝试进行GPU并行化实现。

GPU（图形处理单元）具有大量的并行计算核心，非常适合于数据并行的计算任务。NTT算法中的蝶形操作具有良好的并行性，使其成为GPU加速的理想候选。

实验中，我将首先分析NTT算法的并行化方案，设计合适的GPU线程划分策略；随后实现基于CUDA的NTT算法，包括基础版本和优化版本；最后通过性能测试对比CPU和GPU版本的效果，分析加速比和优化效果。

\paragraph{实验环境} 本次实验在配备NVIDIA GeForce RTX 4060 Laptop GPU的个人计算机系统上进行，使用自行配置的CUDA 12.5进行开发，编译器为nvcc。

\paragraph{实验设计} 本实验基于64位数据类型实现，测试数据使用项目提供的标准测试样例。

\section{基本原理}

\subsection{GPU并行计算模型}

GPU采用SIMT（Single Instruction, Multiple Threads）并行计算模型，通过大量的轻量级线程来执行相同的指令操作不同的数据。GPU的硬件架构包含多个流式多处理器（SM），每个SM包含多个CUDA核心。

在CUDA编程模型中，线程被组织为网格（Grid）和线程块（Block）的层次结构。线程块内的线程可以通过共享内存进行通信和同步，而不同线程块之间的线程无法直接同步。

\subsection{CPU与GPU并行策略的根本差异}

要理解本次GPU编程实验的核心思路，必须首先认识CPU多线程编程与GPU编程在并行粒度选择上的根本差异。在Lab3多线程实验中，我们分析了NTT算法的并行化策略，但GPU编程需要完全不同的思路。

\begin{algorithm}[htbp]
\caption{NTT算法的三层循环结构（来自Lab3分析）}
\label{alg:ntt_structure}
\begin{algorithmic}[1]
\Require 多项式系数数组 $a$, 长度 $n$, 模数 $p$, 原根 $\omega$
\Ensure 变换后的频域系数 $a$
\For{$mid = 1, 2, 4, \ldots, n/2$} \Comment{外层循环：必须串行}
    \State $W_n \gets \omega^{(p-1)/(2 \cdot mid)}$
    \For{$j = 0, 2 \cdot mid, 4 \cdot mid, \ldots, n - 2 \cdot mid$} \Comment{中层循环：可并行}
        \State $w \gets 1$
        \For{$k = 0, 1, 2, \ldots, mid - 1$} \Comment{内层循环：有循环携带依赖}
            \State $x \gets a[j + k]$
            \State $y \gets w \cdot a[j + k + mid]$
            \State $a[j + k] \gets x + y$
            \State $a[j + k + mid] \gets x - y$
            \State $w \gets w \cdot W_n$ \Comment{关键依赖：w依赖于前一次迭代}
        \EndFor
    \EndFor
\EndFor
\end{algorithmic}
\end{algorithm}

\paragraph{CPU多线程的粗粒度并行特征} Lab3的分析表明，CPU多线程编程应该对\textbf{第二层j循环进行并行化}。具体而言，外层mid循环存在强数据依赖，必须串行执行；中层j循环中不同j块操作的数据区域完全独立，适合中等粒度并行化；内层k循环的旋转因子w具有循环携带依赖（$w_{k+1} = w_k \cdot W_n$），破坏依赖会导致算法错误。

这种策略的核心思想是：\textbf{CPU线程创建和调度开销较大，因此不宜将任务划分过细}。每个线程负责一个完整的j块计算（包含mid个蝶形操作），在线程内部串行执行k循环以保持数学正确性。

\paragraph{GPU编程的极细粒度并行需求} 与CPU多线程截然不同，GPU架构具有以下特征。\textbf{大规模并行计算单元}方面，现代GPU拥有数千个CUDA核心，需要大量线程才能充分利用硬件资源；\textbf{极低的线程调度开销}方面，GPU线程调度由硬件实现，单个线程的创建和切换成本极低；\textbf{SIMT执行模型}方面，线程以warp（32线程）为单位同步执行，最适合数据并行的细粒度操作。

因此，GPU编程的核心原则是：\textbf{应当将计算任务划分到最细的粒度，让每个线程处理最小的数据单元}。对于NTT算法，这意味着每个线程应该处理一个蝶形操作，而不是一个j块。

\subsection{GPU并行化的核心挑战：循环携带依赖的消除}

将CPU的粗粒度并行策略直接移植到GPU会严重浪费计算资源。真正的GPU并行化需要解决内层k循环的依赖问题，实现真正的极细粒度并行。

\paragraph{依赖问题分析} 在原始算法中，旋转因子w的计算存在严格的循环携带依赖：
$$w_0 = 1, \quad w_{k+1} = w_k \cdot W_n$$

这种依赖关系使得第k+1次蝶形操作必须等待第k次操作完成，无法并行执行。

\paragraph{依赖消除策略} GPU并行化的关键突破是认识到：每个蝶形操作所需的旋转因子可以独立计算。对于第k个蝶形操作，其旋转因子为：
$$w_k = W_n^k = \left(\omega^{(p-1)/(2 \cdot mid)}\right)^k$$

通过预计算所有可能的旋转因子并存储在GPU显存中，每个线程可以直接索引获取所需的w值，完全消除了循环携带依赖。

\subsection{极细粒度GPU并行化设计}

基于依赖消除策略，我们设计了真正的GPU并行化方案：

\paragraph{线程映射策略} 为每个蝶形操作分配一个GPU线程，实现完全的j、k循环并行化。\textbf{外层mid循环}保持在CPU主机端串行控制（数据依赖不可消除）；\textbf{中层j循环 + 内层k循环}完全扁平化为线程网格，每个线程处理一个蝶形操作；\textbf{旋转因子获取}通过预计算表直接索引，消除w的依赖关系。

\paragraph{线程索引计算} 每个GPU线程通过全局索引计算其负责的蝶形操作：

\begin{algorithm}[htbp]
\caption{GPU极细粒度并行化核心算法}
\label{alg:gpu_fine_grain}
\begin{algorithmic}[1]
\Require GPU数组 $a$, 预计算旋转因子表 $twiddles$, mid级别
\Ensure 完成所有蝶形操作的并行执行
\State $tid \gets blockIdx.x \times blockDim.x + threadIdx.x$
\If{$tid \geq n/2$} \textbf{return} \Comment{超出蝶形操作总数}
\EndIf
\State $step \gets 2 \times mid$
\State $j \gets (tid \div mid) \times step$ \Comment{确定j块位置}
\State $k \gets tid \bmod mid$ \Comment{确定块内k位置}
\State $tw\_idx \gets (mid - 1) + k$ \Comment{旋转因子表索引}
\State $w \gets twiddles[tw\_idx]$ \Comment{直接获取预计算的旋转因子}
\State $x \gets a[j + k]$
\State $y \gets w \times a[j + k + mid]$
\State $a[j + k] \gets x + y$
\State $a[j + k + mid] \gets x - y$
\end{algorithmic}
\end{algorithm}

这种设计实现了真正的极细粒度并行：对于长度为n的多项式，每个mid级别需要n/2个蝶形操作，我们启动n/2个线程并行执行，充分利用GPU的大规模并行计算能力。

\section{搭建实现}

\subsection{旋转因子预计算策略}

实现极细粒度GPU并行化的关键是消除旋转因子的循环携带依赖。我们通过预计算所有可能的旋转因子来解决这个问题。

\paragraph{旋转因子表生成} 对于每个mid级别，我们预计算该级别所需的所有旋转因子：

\begin{lstlisting}[language=C++]
template <typename T>
void generate_twiddle_table(std::vector<T> &table, T n, T p, T omega, bool inverse) {
    table.resize(n);
    MontMod<T> mont(p);
    T omega_mont = mont.from_T(omega);
    if (inverse) omega_mont = mont.inv(omega_mont);
    T one_mont = mont.from_T(1);

    for (int level = 0; (T(1) << level) < n; ++level) {
        T mid = T(1) << level;
        T exp = (p - 1) / (mid << 1);
        T Wn_mont = mont.pow(omega_mont, exp);
        T w = one_mont;
        T offset = mid - 1;
        for (T k = 0; k < mid; ++k) {
            table[offset + k] = w;
            w = mont.mul(w, Wn_mont);
        }
    }
}
\end{lstlisting}

通过这种预计算策略，每个线程可以通过简单的索引操作获取其所需的旋转因子，而无需进行复杂的模幂运算或依赖其他线程的计算结果。

\subsection{极细粒度并行核函数}

基于预计算的旋转因子表，我们实现了真正的极细粒度并行核函数：

\paragraph{核函数设计思路} 每个GPU线程负责处理一个蝶形操作，通过全局线程索引计算其在NTT算法中的位置，并直接索引获取所需的旋转因子。

\begin{lstlisting}[language=C++]
template <typename T, int MODE>
__global__ void ntt_stage_kernel_fwd_mode(T *a, const T *twiddles, int mid, int n, T p, T neg_r_inv, u64 mu) {
    int tid = blockIdx.x * blockDim.x + threadIdx.x;
    int total = n >> 1;
    if (tid >= total) return;

    int step = mid << 1;
    int j = (tid / mid) * step;        // 确定j块位置
    int k = tid % mid;                 // 确定块内k位置
    int tw_idx = (mid - 1) + k;        // 旋转因子表索引
    T w_mont = twiddles[tw_idx];       // 直接获取预计算的旋转因子

    T x = a[j + k];
    T y = mod_mul_unified<MODE>(w_mont, a[j + k + mid], p, neg_r_inv, mu);
    a[j + k] = gpu_mont_add_correct(x, y, p);
    a[j + k + mid] = gpu_mont_sub_correct(x, y, p);
}
\end{lstlisting}

这种实现的关键优势在于：每个线程完全独立，无需等待其他线程的计算结果；所有蝶形操作可以真正并行执行；充分利用GPU的大规模并行计算能力。

\paragraph{线程映射分析} 对于长度为n的多项式，每个mid级别需要n/2个蝶形操作。我们的线程映射策略如下：
\begin{algorithmic}[1]
\State $total\_butterflies \gets n/2$ \Comment{总蝶形操作数}
\State $BLOCK\_SIZE \gets 256$ \Comment{每个线程块包含256个线程}
\State $GRID\_SIZE \gets \lceil total\_butterflies / BLOCK\_SIZE \rceil$ \Comment{网格大小}
\State 启动 $GRID\_SIZE \times BLOCK\_SIZE$ 个线程并行执行
\end{algorithmic}

\subsection{多模乘算法支持}

为了深入分析GPU编程中不同算法的性能特征，我们实现了三种模乘算法的统一接口，通过模板参数MODE在编译时选择具体的模乘算法，避免运行时的分支开销，同时便于性能对比分析。

\begin{lstlisting}[language=C++]
template <int MODE, typename T>
__device__ inline T mod_mul_unified(T a, T b, T p, T neg_r_inv, u64 mu) {
    if constexpr (MODE == MUL_NAIVE) {
        return mod_mul_naive<T>(a, b, p);
    } else if constexpr (MODE == MUL_MONT) {
        return gpu_mont_mul_correct(a, b, p, neg_r_inv);
    } else { // MUL_BARRETT
        return mod_mul_barrett_32(a, b, p, mu);
    }
}
\end{lstlisting}

\subsection{主机端控制逻辑}

主机端负责外层mid循环的串行控制，并为每个级别启动大规模并行的GPU计算。每次核函数启动都处理n/2个蝶形操作，而不是像传统CPU思路那样逐个处理j块。

这里以Montgomery规约模乘为例，其余两种模乘代码同理。

\begin{lstlisting}[language=C++]
template <typename T, int MODE>
void poly_multiply_ntt_gpu_core(T *a, T *b, T *ab, T n, T p, T omega) {
    MontMod<T> montMod(p);
    T n_expanded = expand_n(2 * n - 1);
    
    // 预计算旋转因子表
    std::vector<T> tw_host(n_expanded);
    std::vector<T> tw_inv_host(n_expanded);
    generate_twiddle_table(tw_host, n_expanded, p, omega, false);
    generate_twiddle_table(tw_inv_host, n_expanded, p, omega, true);

    // 上传旋转因子表到GPU
    T *tw_gpu = nullptr;
    T *tw_inv_gpu = nullptr;
    CHECK_CUDA_CORRECT(cudaMalloc(&tw_gpu, n_expanded * sizeof(T)));
    CHECK_CUDA_CORRECT(cudaMalloc(&tw_inv_gpu, n_expanded * sizeof(T)));
    CHECK_CUDA_CORRECT(cudaMemcpy(tw_gpu, tw_host.data(), n_expanded * sizeof(T), cudaMemcpyHostToDevice));
    CHECK_CUDA_CORRECT(cudaMemcpy(tw_inv_gpu, tw_inv_host.data(), n_expanded * sizeof(T), cudaMemcpyHostToDevice));

    // NTT前向变换：真正的极细粒度并行
    const int BLOCK = 256;
    for (int level = 0; (T(1) << level) < n_expanded; ++level) {
        T mid = T(1) << level;
        int total_butterfly = n_expanded >> 1;      // 总蝶形操作数
        int GRID = (total_butterfly + BLOCK - 1) / BLOCK;  // 网格大小

        ntt_stage_kernel_fwd_mode<T, MODE><<<GRID, BLOCK>>>(a_mont_gpu, tw_gpu, mid, n_expanded, p, neg_r_inv, mu);
        ntt_stage_kernel_fwd_mode<T, MODE><<<GRID, BLOCK>>>(b_mont_gpu, tw_gpu, mid, n_expanded, p, neg_r_inv, mu);
        CHECK_CUDA_CORRECT(cudaGetLastError());
    }
    CHECK_CUDA_CORRECT(cudaDeviceSynchronize());
    
    // 点乘和逆变换...
}
\end{lstlisting}

\section{实验结果}

\subsection{实验环境}

本次实验在NVIDIA GeForce RTX 4060 Laptop GPU上进行，使用CUDA 12.5编程模型。我们首先对教师提供的测试规模（n=4和n=131072）进行测试，随后扩展到更多中间规模以全面理解GPU性能特征。

我们在此首先测试基于Montgomery规约的GPU代码。

\subsection{教师样例测试}

对于教师提供的测试点，CPU和GPU版本的计算结果完全一致，验证了算法正确性。我们多次运行代码并计时，得到测试结果如表\ref{tab:official_results}所示。

\begin{table}[htbp]
\centering
\caption{Official Test Scale Results}
\label{tab:official_results}
\begin{tabular}{|c|c|c|c|c|}
\hline
Problem Size & CPU Time (μs) & GPU Time (μs) & Speedup & Performance Assessment \\
\hline
n=4 & 3.4 & 1,792 & 0.002 & CPU 526x faster \\
\hline
n=131072 & 86,532 & 62,647 & 1.381 & GPU 1.38x faster \\
\hline
\end{tabular}
\end{table}

测试结果显示，在小规模问题上，GPU启动开销使其性能远不如CPU；在大规模问题上，GPU也仅比CPU快38\%。

这很奇怪：GPU的性能理论上会比CPU快数十倍。因此，我们尝试对代码进行进一步测试以找出问题所在。

\subsection{全规模测试}

我们将测试范围从n=4扩展到n=1,048,576（1M），共19个测试点，进行测试并将结果可视化。图\ref{fig:comprehensive_performance_analysis}展示了从n=4到n=1M的完整性能曲线：

\begin{figure}[htbp]
\centering
\includegraphics[width=1.0\textwidth]{image/comprehensive_performance_analysis.png}
\caption{NTT Performance Analysis: CPU vs GPU Across Full Scale Range (n=4 to n=1,048,576)}
\label{fig:comprehensive_performance_analysis}
\end{figure}

该图使用对数-对数刻度展示了所有规模下的性能特征。可以观察到CPU时间随问题规模呈现典型的对数线性增长趋势，而GPU时间在小规模时受启动开销影响较大，在大规模时逐渐体现并行优势。

\begin{table}[htbp]
\centering
\caption{Comprehensive Performance Test Results}
\label{tab:comprehensive_performance}
\begin{tabular}{|c|c|c|c|}
\hline
Problem Size (n) & CPU Time (μs) & GPU Time (μs) & Speedup \\
\hline
4 & 3.4 & 1792 & 0.002 \\
\hline
64 & 16.5 & 2192 & 0.008 \\
\hline
1,024 & 367.2 & 2700 & 0.136 \\
\hline
16,384 & 8482 & 10673 & 0.795 \\
\hline
65,536 & 37669 & 34644 & 1.087 \\
\hline
131,072 & 86532 & 62647 & 1.381 \\
\hline
262,144 & 189410 & 137184 & 1.381 \\
\hline
524,288 & 395680 & 273409 & 1.447 \\
\hline
1,048,576 & 993007 & 651165 & 1.525 \\
\hline
\end{tabular}
\end{table}
基于全规模测试，我们发现，之前观察到的GPU性能仅略微优于CPU的原因并不是数据规模不足。在大规模阶段（32768<n≤131072），加速比略大于1且随问题规模大致呈线性增加，并不存在随数据规模增大而急速增加的趋势。

这说明我们的GPU性能不达预期的原因应该在于代码本身。

\section{问题查找与分析}

通过对实验结果的深入分析以及对GPU代码实现的仔细检查，我们发现了导致GPU性能不达预期的几个可能原因，解释了为什么GPU的加速比仅为1.5倍左右。

\subsection{线程配置策略}

\paragraph{固定线程块大小} GPU实现使用了固定的256线程块大小（\texttt{const int BLOCK = 256}），这种一刀切的配置策略存在明显缺陷：

对于\textbf{小规模问题}（n<16384），GPU启动256个线程块的开销远大于实际计算量，导致严重的资源浪费；对于\textbf{大规模问题}，256可能不是最优的线程块大小，未能充分利用GPU的计算单元；缺乏\textbf{自适应配置机制}，无法根据问题规模和硬件特性动态调整线程组织方式。

\paragraph{线程利用率分析} 以n=65536为例，总计需要32768个蝶形操作，启动128个线程块。然而，对于更小的规模如n=1024，仅需512个蝶形操作，启动2个线程块，大量计算单元处于空闲状态。

\subsection{内存访问模式}

\paragraph{内存层次结构} 当前实现直接从全局内存访问旋转因子表，未充分利用GPU的内存层次结构：

\textbf{旋转因子表访问}方面，每个线程独立从全局内存读取旋转因子，缺乏缓存友好的访问模式；\textbf{共享内存利用不足}方面，未使用共享内存预加载频繁访问的数据；\textbf{内存带宽浪费}方面，随机的全局内存访问模式无法实现合并访问，浪费了GPU的高内存带宽。

\paragraph{数据局部性} NTT算法的蝶形操作具有良好的数据局部性，但当前实现未能有效利用这一特征。相邻的线程可能访问相同的旋转因子，但每个线程都独立进行全局内存访问。

\subsection{算法正确性验证}

\paragraph{数值正确性检查} 尽管存在上述性能问题，我们通过与CPU版本的结果对比验证了GPU实现的数值正确性。所有测试用例的计算结果都与CPU版本完全一致，说明算法的核心逻辑是正确的。

\section{总结}

由于本次实验前半部分的绝大部分时间用来学习CUDA编程的原理以及代码编写方法，而后半部分时间在尝试找出性能瓶颈上浪费了大量时间，因此本次实验不得不遗憾地把优化留到下一次实验中进行。

然而，本次实验虽然没有达到预期的优化效果，却也成功实现了基于CUDA的NTT算法GPU并行化，深入探索了CPU多线程与GPU编程在并行策略设计上的根本差异。通过理论分析、代码实现和性能测试，我们取得了以下重要成果和深刻认识。

\subsection{核心技术贡献}

\paragraph{极细粒度并行策略的成功设计} 我们突破了传统CPU多线程的粗粒度并行思维，成功设计了真正适合GPU架构的极细粒度并行策略。通过预计算旋转因子表彻底消除了NTT算法内层循环的循环携带依赖，实现了每个线程处理一个蝶形操作的完全并行化。这一策略的核心创新在于：将CPU的中等粒度并行（每线程处理一个j块）转换为GPU的极细粒度并行（每线程处理一个蝶形操作）。

\paragraph{多模乘算法的统一框架} 建立了支持朴素模乘、Montgomery规约和Barrett规约的统一GPU实现框架，为不同应用场景下的算法选择提供了灵活性。虽然当前实现存在一些技术细节问题，且由于时间仓促未能来得及编写基于朴素模乘和Barrett规约的GPU代码，但框架设计为后续优化奠定了良好基础。

\subsection{未来改进方向}

基于本次实验的分析和发现，我们确定了以下主要的改进方向：设计自适应的线程配置策略，根据问题规模动态优化资源配置；优化内存访问模式，充分利用GPU的内存层次结构。

总体而言，本次实验成功实现了NTT算法的GPU并行化，并且深化了我们对GPU并行计算本质的理解，为后续的进一步优化奠定了坚实的理论和实践基础。

\end{document}